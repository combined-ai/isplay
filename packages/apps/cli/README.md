# isplay

Command-line interface for isplay.

The `isplay` bin can start the local API and worker, create projects and runs, manage branches and replays, inspect experiments, and stream durable job events.
