# @isplay/adapter-kit

Shared adapter helpers for framework integrations.

Adapters must receive an explicit `client` or `projectId`; pass `getClient()` yourself if you want SDK environment convenience.
