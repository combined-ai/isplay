# @isplay/adapter-runtime

Runtime classification helpers for isplay adapters.

It classifies runtime events and side effects conservatively so unknown or shell-like tools are not treated as safe writes.
